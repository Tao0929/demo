1. 新增语种 (命令行输入):
- npm run add-lang
- 根据提示输入 语种中文名称(如: 中文) 
- 根据提示输入 语种英文名称(如: zh-CN) 
- 回车即可 自动在 i18n 的 lang目录和page目录下 生成 语种文件;

2. 批量编辑语种变量(命令行输入):
 - npm run modify-lang 
 - 根据输入提示输入 需要改变的 i18n变量 (如: ui.app.name )
 - 根据输入提示输入 需要改变的 i18n变量 的值 (如: 中国移动)
 - 回车即可 自动在 所有的语种文件中 改变 该变量的值;
